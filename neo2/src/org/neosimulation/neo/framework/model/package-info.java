/**
 * This package contains the classes which either encapsulate the structure
 * (matrix or network), build the structure (NetworkBuilder), manage connections
 * (Dependency and DependencyManager and StateValContainer and StateValUpdater),
 * or represent a model itself (SimulationModel and NEOSimulationModel).
 */
package org.neosimulation.neo.framework.model;